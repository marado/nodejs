git-buildpackage
================

Upstream tarball sometimes contains *.orig files that are left over by
upstream patches. Since dh_clean will remove them, they should be protected
by using DEB_CLEAN_EXCLUDE in debian/rules.

